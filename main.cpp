#include <iostream>
#include "AVL.h"

int main() {
    AVL t;

    // read in the number of commands
    // for each command, execute it on t

    // what we want to do:
    t.Insert("Jesus", "123456789");

    return 0;
}
